<view class="container">
    <block wx:for="{{thingsGroup}}" wx:key="index">
        <l-card type="cover" image="{{item.cover}}" title="{{item.title}}">
            <view class="content">
                {{item.description}}
            </view>
        </l-card>
    </block>
</view>